<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>
    Atualizar Questão
  </title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
</head>

<body>
  <main class="page">
    <div class="container">
      <header class="page-header">
        <h1>Responder Questão</h1>
    </header>

      <form class="card form-card" action="/aluno/responder" method="POST">
        <div class="field">
          <div class="field">
            <label hidden>Id</label>
            <input type="hidden" name="id" value="<%= exercicio.id %>">
          </div>

          <div class="field">
            <label>Descrição / Enunciado</label>
            <textarea id="descricao" name="descricao" value="<%= exercicio ? exercicio.descricao : '' %>"readonly> </textarea>
          </div>

          <div class="field">
            <label>Resultado esperado (texto exato que será usado para verificar)</label>
            <textarea id="resposta" name="resposta" rows="3" placeholder="Ex.: 42"></textarea>
            <small class="help">O sistema fará a comparação de acordo com a lógica que você
              implementar (ex.: trim, lowercase, números, etc.).</small>
          </div>

          <div class="form-actions">
            <button class="btn btn-primary" type="submit">Salvar alterações</button>
            <a class="btn btn-ghost" href="<%= '/professor/listar' %>">Cancelar</a>
          </div>
      </form>
    </div>
  </main>

</body>

</html>